<UserControl x:Class="LetsGetOrganizedWPF.UserControls.OptionsPageTwo.PresetCustomToggle"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:LetsGetOrganizedWPF.UserControls.OptionsPageTwo"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="0" Margin="0, 0, 0 ,15">
            <TextBlock Style="{StaticResource StepHeaderText}"
                Text="Step 2: Choose your organization style"/>
          
        </StackPanel>
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Center" Grid.Row="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                    Style="{StaticResource SectionHeaderText}"
                    Text="Preset"
                    VerticalAlignment="Center"
                    Margin="0,0,10,0"/>
                <ToggleButton Grid.Column="1"
                    x:Name="PresetCustomToggleButton"
                    Style="{StaticResource ToggleSwitchStyle}"
                    IsChecked="false"
                    Unchecked="ToggleSetPreset"
                    Checked="ToggleSetCustom">
                </ToggleButton>
                <TextBlock Grid.Column="2"
                    Style="{StaticResource SectionHeaderText}"
                    Text="Custom"
                    VerticalAlignment="Center"
                    Margin="10,0,0,0"/>
            </Grid>
            
        </StackPanel>
    </Grid>
</UserControl>
